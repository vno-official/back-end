# Configuration is managed by Config Server
# See: config-repo/auth-service.yml

# Local overrides (optional)
# Uncomment below to override config-server settings for local development

# server:
#   port: 8081

# spring:
#   datasource:
#     url: jdbc:postgresql://localhost:5432/vno_auth
#     username: postgres
#     password: postgres
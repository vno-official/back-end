asyncapi: 3.0.0
info:
  title: User Events
  version: 1.0.0
servers:
  kafka-local:
    url: localhost:9092
    protocol: kafka
channels:
  user.created:
    messages:
      userCreated:
        payload:
          type: object
          properties:
            id: { type: string }
